// For Loops

// Program to add first n natural numbers
const prompt = require("prompt-sync")();
/* let sum = 0
let n = prompt("Enter the value of n")
n = Number.parseInt(n)
for (let i = 0; i < n; i++) {
    sum += (i + 1);
    // console.log(sum)
    // console.log(i+1) // to start from 1
}
console.log("Sum of first " + n + " natural numbers is " + sum) */
// console.log(i) - it shows i is not defined because we use let keyword which is block scoped


let obj = {
    Aryan: 95,
    Gayle: 50,
    Russell: 45,
    Dhoni: 80,
    Kholi: 90
} 


// For in Loop
/*
for (let a in obj) {
    console.log("Score of " + a + " are " +obj[a])
    // console.log(a)
} */

// For of Loop - needs iterable objects(Arrays & Strings)
/* for (let b of "Harry"){
    console.log(b)
} */
 